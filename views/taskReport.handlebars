

<section class="bg-white text-dark pt-5 pb-5">
    <div class="container">
        <div class="row text-center">
            <div class="col">
                <h2 class="  mb-2"><strong>Task Report</strong></h1>
                <h4 class="  mb-2">Here you can find all of the tasks that need completion.</h4>
            </div>
            <div>
          
            </div>
        </div>
        <div class="row text-center mt-md-5">
            <div class="col-md-3 mb-5 col-12">
                <img alt="image" style="max-width:150px;" class="img-fluid mb-4 mt-3 rounded-circle img-rised"
                    src="red-clock.png">
                <h3><u><strong>Overdue</strong></u></h3>
            {{#each tasks}}

            {{#if overdue}}
            <hr>
            <h5><strong>{{shortname}}</strong></h5>
            {{!-- <p>Description: {{description}}</p> --}}
            <p class="mb-0">Due Date: {{dueDate}}</p>
            {{!-- <button class="change-complete" data-id="{{id}}" data-newsleep="{{sleep}}">
		    {{#if completed}}Complete!{{else}}Re-do!{{/if}}
	        </button> --}}
            <hr>
            {{/if}}

            {{/each}}


            </div>
            <div class="col-md-3 mb-5 col-12">
                <img alt="image" style="max-width:150px;" class="img-fluid mb-4 mt-3 rounded-circle img-rised"
                    src="orange-clock.png">
                <h3><u><strong>Due within 3 days</strong></u></h3>
                {{#each tasks}}

            {{#if lessThan3}}
            <hr>
            <h5><strong>{{shortname}}</strong></h5>
            {{!-- <p>Description: {{description}}</p> --}}
            <p>Due Date: {{dueDate}}</p>
            {{!-- <button class="change-complete" data-id="{{id}}" data-newsleep="{{sleep}}">
		    {{#if completed}}Complete!{{else}}Re-do!{{/if}}
	        </button> --}}
            <hr>
            {{/if}}

            {{/each}}

                
            </div>
            <div class="col-md-3 mb-5 col-12">
                <img alt="image" style="max-width:150px;" class="img-fluid mb-4 mt-3 rounded-circle img-rised"
                    src="yellow-clock.png">
                <h3><u><strong>Due within 7 days</strong></u></h3>
            {{#each tasks}}

            {{#if lessThan7}}
            <hr>
            <h5><strong>{{shortname}}</strong></h5>
            <p>Due Date: {{dueDate}}</p>
            {{!-- <button class="change-complete" data-id="{{id}}" data-newsleep="{{sleep}}">
		    {{#if completed}}Complete!{{else}}Re-do!{{/if}}
	        </button> --}}
            <hr>
            {{/if}}

            {{/each}}

                
            </div>
            <div class="col-md-3 mb-5 col-12">
                <img alt="image" style="max-width:150px;" class="img-fluid mb-4 mt-3 rounded-circle img-rised"
                    src="green-clock.png">
                <h3><u><strong>Due in over a week</strong></u></h3>
            {{#each tasks}}

            {{#if oneWeek}}
            <hr>
            <h5><strong>{{shortname}}</strong></h5>
            <p>Due Date: {{dueDate}}</p>
            {{!-- <button class="change-complete" data-id="{{id}}" data-newsleep="{{sleep}}">
		    {{#if completed}}Complete!{{else}}Re-do!{{/if}}
	        </button> --}}
            <hr>
            {{/if}}

            {{/each}}
               
            </div>
        </div>
    </div>
</section>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js" type="text/javascript"></script>

{{!-- <script src="../public/js/moment.min.js" type="text/javascript"> --}}
<script>

</script>